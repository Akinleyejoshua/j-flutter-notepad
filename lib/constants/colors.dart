import 'package:flutter/material.dart';

const royalblue = Color.fromARGB(255, 0, 110, 255);
const white = Colors.white;
const red = Colors.red;
const green = Colors.green;
const black = Color.fromARGB(80, 0, 0, 0);
